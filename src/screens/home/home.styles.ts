import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingTop: 60,
  },
  logo: {
    height: 150,
    maxWidth: '60%',
    resizeMode: 'contain',
  },
  buttons: {
    marginTop: 30,
    gap: 8,
  },
});

export default styles;
